pipeline {
    agent any
    stages {
        stage('pull recipeints') {
            steps {
                script {
                    def jss = groovy.json.JsonSlurper()
                    def orecp = jss.parseText("${recipients}");
                    Map mrecp = (Map) orecp;
                    mrecp.each { key, value ->
                    println key;
                    println value;
                    //println "${recipients}"
                    }
                }
            }
        }
        //stage('send test mail') {
        //    steps {
        //       mail body: 'testy testy testington',
        //        subject: 'test email from Jenkins',
        //        to: 'andrewsampson738@gmail.com'
        //    }
        //}
    }
}